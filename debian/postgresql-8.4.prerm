#!/bin/sh -e

VERSION=8.4

#DEBHELPER#

. /usr/share/postgresql-common/maintscripts-functions

if [ "$1" = upgrade ]; then
    stop_version $VERSION
fi

if [ "$1" = remove ]; then
    remove_version $VERSION
fi

