#!/bin/sh
set -e

# earlier releases used per-version init scripts, now replaced with common one 
if [ "$1" = "upgrade" ] || [ "$1" = "install" ] && \
	dpkg --compare-versions "$2" le-nl "8.4.4-2"; then
    rm_conffile /etc/init.d/postgresql-8.4
fi

#DEBHELPER#

exit 0
